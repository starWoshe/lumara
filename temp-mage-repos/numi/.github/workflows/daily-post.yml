name: Daily Post — Numi

on:
  schedule:
    - cron: '0 10 * * *'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: packages/agents/numi/scripts/requirements.txt
      - run: pip install -r packages/agents/numi/scripts/requirements.txt
      - run: python packages/agents/numi/scripts/daily_bot.py
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          NUMI_PAGE_ACCESS_TOKEN: ${{ secrets.NUMI_PAGE_ACCESS_TOKEN }}
          NUMI_PAGE_ID: ${{ secrets.NUMI_PAGE_ID }}
          NUMI_IG_USER_ID: ${{ secrets.NUMI_IG_USER_ID }}
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
          NUMI_TELEGRAM_BOT_TOKEN: ${{ secrets.NUMI_TELEGRAM_BOT_TOKEN }}
          NUMI_TELEGRAM_CHANNEL_ID: ${{ secrets.NUMI_TELEGRAM_CHANNEL_ID }}
